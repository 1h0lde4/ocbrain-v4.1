"""core/capabilities/adapters/ — concrete Adapter implementations.

Each adapter fulfills exactly one CapabilityType by wrapping exactly one
external system, per the Kernel Constitution's Adapter definition
(Part VII: "Translate one specific external system... into the kernel's
capability contract. Disposable and replaceable by design.").
"""
