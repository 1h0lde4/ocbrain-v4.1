"""eval_lab/tests — contract-level test suite for Slice 2.

Not auto-discovered by a bare `pytest` invocation at repo root (see
eval_lab/README.md: root pyproject.toml scopes testpaths to `tests/`, and
this Slice deliberately did not modify shared repo configuration). Run
explicitly: `pytest eval_lab/tests -v`.
"""

from __future__ import annotations
